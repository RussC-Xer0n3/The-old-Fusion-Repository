//This one jumps quite rapidly at some stage, need a secured offline env to start looking at this a bit closer

/**
* Some theoretical physics regarding fusion to pass the day by. 
* @Author Russell Clarke SD200984 aka Rusher
* Listening to 'I wish I had Duck Feet' by Orbital.
* v3.3.1
*/

import java.math.*;

public class Main
{
	public static void main(String[] args)
	{
		double va = Math.PI;
		System.out.println(va); /* Check the value of PI in Java Math */
	
		/* Let's set a radius (in nm) and imagine it is constrained
		 * as a small ball of energy to see what happens. 
		 */
		double r = 0.01;
		
		/* ORIGINAL : Double q = (va*(r))*3; \/\/PI cubed since it needs to be. */
		double q = (va*(r*3));
		
		System.out.println(q); /* Output the current value in nm3. */
		
		
		
		/* THE ALGORITHM PART */

		for(double i = ((q)/(q*(q*q)*-q)); i<=i; i++){
			System.out.println(i);
			/* Though technically there should only be energy as the output, */
			/* the numbers should start to represent what's happening inside and out. */
		}
	}
}
